server:
  port: 7788
spring:
  application:
    name: feign
eureka:
  instance:
    hostname: localhost
    port: 7777
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/