server:
  port: 7780
spring:
  application:
    name: ribbon
eureka:
  instance:
    hostname: localhost
    port: 7777
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/